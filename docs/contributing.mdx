---
title: "Contributing"
description: "How to contribute to Agent Army"
---

Agent Army is open source. Contributions are welcome!

## Getting Started

```bash
# Clone the repo
git clone https://github.com/stepandel/agent-army.git
cd agent-army

# Install dependencies
pnpm install

# Build the CLI
pnpm --filter agent-army build
```

## Project Structure

- `cli/` — CLI package (commands, config, types, registries)
- `src/` — Pulumi infrastructure components
- `identities/` — Built-in identity stubs (point to external Git repos)
- `examples/` — Example identities for reference
- `docs/` — Mintlify documentation

## Development Workflow

1. Create a feature branch from `main`
2. Make your changes
3. Run `pnpm build` to verify
4. Submit a PR with a clear description

## Conventions

- **Commits:** Use [conventional commits](https://www.conventionalcommits.org/) (`feat:`, `fix:`, `docs:`, etc.)
- **PRs:** One PR per feature/fix, link to relevant issues
- **TypeScript:** Strict mode, no `any` without comment

## Adding a New Built-in Identity

1. Create an identity directory with `identity.yaml` and workspace files (see [examples/identity/](https://github.com/stepandel/agent-army/tree/main/examples/identity) for the format)
2. Host the identity in a Git repo (or add to an existing identities repo)
3. Register it in `cli/lib/constants.ts` under `BUILT_IN_IDENTITIES`
4. Update documentation in `docs/architecture/identities.mdx`

## Adding a New Cloud Provider

1. Create a new component in `src/components/`
2. Add provider constants to `cli/lib/constants.ts`
3. Update the CLI adapter and web wizard
4. Document in `docs/architecture/cloud-providers.mdx`

## Adding a Registry Entry

- **Coding agent:** Add to `cli/lib/coding-agent-registry.ts` with install script and model config
- **Dep:** Add to `cli/lib/dep-registry.ts` with install script
- **Plugin:** Add to `cli/lib/plugin-registry.ts` with metadata
